[project]
name = "antibody-fixture"
version = "0.1.0"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-ra"

[tool.vercel]
entrypoint = "api.v1:handler"
